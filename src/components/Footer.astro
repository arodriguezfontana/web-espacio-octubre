---
import { siteData } from '../data/siteData';
import { navItems } from '../data/navigationData';
import Icon from './ui/Icon.astro';
import CtaBanner from './ui/CtaBanner.astro';

const currentYear = new Date().getFullYear();
---

<footer class="bg-primary-dark text-slate-300 font-sans tracking-tight">
  <CtaBanner />

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 md:py-14 border-b border-white/10">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 lg:gap-12 items-start justify-between">
      
      <div class="space-y-4">
        <div class="flex flex-col border-l-2 border-l-accent pl-3.5">
          <span class="text-2xl font-bold text-white tracking-tight">
            {siteData.name}
          </span>
          <span class="text-xs font-medium text-secondary-light tracking-wide mt-0.5">
            {siteData.subtitle}
          </span>
        </div>

        <p class="text-slate-300/90 text-xs sm:text-sm leading-relaxed max-w-sm font-normal">
          {siteData.description}
        </p>

        <div class="pt-1 flex items-center gap-2 text-xs text-accent-extra-light font-medium">
          <span class="w-2 h-2 rounded-full bg-accent animate-pulse shrink-0"></span>
          <span>{siteData.contact.modality}</span>
        </div>
      </div>

      <div class="space-y-3 md:justify-self-center">
        <h3 class="text-xs font-semibold text-white uppercase tracking-wider">
          Navegación
        </h3>
        <ul class="space-y-2.5 text-xs sm:text-sm">
          {navItems.map((item) => (
            <li>
              <a 
                href={item.href} 
                class="inline-flex items-center gap-2 text-slate-300 hover:text-white hover:underline transition-colors group/nav"
              >
                <Icon name="arrow-right" class="w-3 h-3 text-secondary-light shrink-0 transition-transform group-hover/nav:translate-x-0.5" />
                <span>{item.label}</span>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="space-y-3 md:justify-self-end">
        <h3 class="text-xs font-semibold text-white uppercase tracking-wider">
          Información
        </h3>
        <ul class="space-y-2.5 text-xs sm:text-sm">
          <li class="flex items-start gap-2.5">
            <Icon name="location" class="w-4 h-4 text-secondary-light shrink-0 mt-0.5" />
            <a 
              href={siteData.location.googleMapsExternalUrl} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-slate-300 hover:text-white hover:underline transition-colors font-normal inline-flex items-center"
            >
              <span>{siteData.location.address}, {siteData.location.city}</span>
            </a>
          </li>

          <li class="flex items-center gap-2.5">
            <Icon name="clock" class="w-4 h-4 text-secondary-light shrink-0" />
            <span class="text-slate-300">{siteData.contact.hours}</span>
          </li>

          <li class="flex items-center gap-2.5">
            <Icon name="whatsapp" class="w-4 h-4 text-secondary-light shrink-0" />
            <a 
              href={siteData.contact.whatsappUrl} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-slate-300 hover:text-white hover:underline transition-colors font-medium inline-flex items-center"
            >
              {siteData.contact.phoneDisplay}
            </a>
          </li>

          <li class="flex items-center gap-2.5">
            <Icon name="instagram" class="w-4 h-4 text-secondary-light shrink-0" />
            <a 
              href={siteData.social.instagram} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-slate-300 hover:text-white hover:underline transition-colors font-medium inline-flex items-center"
            >
              @espaciooctubre
            </a>
          </li>

        </ul>
      </div>

    </div>
  </div>

  <div class="py-5 text-xs text-slate-400">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-center sm:text-left">
      <p>© {currentYear} {siteData.name}. Todos los derechos reservados.</p>
    </div>
  </div>

</footer>